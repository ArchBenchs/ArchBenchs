Здесь симейк:

export PATH=/common/home/simonov_m/cmake/bin:$PATH


Этой строкой запускал адвайзор (вроде бы проблем не было)

srun -N 1 advisor --collect=roofline ./LU_Benchmark


Этой строкой замерял время на х86

srun numactl -C 0-19 ./LU_Benchmark --size 10000 --dac


Вся сборка сейчас происходит тулчейнами, все добавил, что было:

cmake .. -DCMAKE_TOOLCHAIN_FILE=../toolchains/risk-V_gcc.cmake
cmake .. -DCMAKE_TOOLCHAIN_FILE=../toolchains/risk-V_clang.cmake
cmake .. -DCMAKE_TOOLCHAIN_FILE=../toolchains/x86_icpx2023.cmake

А также определения:
	-DREFERENCE_TEST=< eigen | mkl >	для сравнения с eigen-5.0.0 или Intel MKL, если установлена в системе
	-DPRINT_BLOCK_TIMES=1			для замеров времени работы каждого этапа разложения по отдельности
	-DBLOCK_SIZE=<какое-то число>		чтобы задать размер блока для блочного алгоритма (в тулчейнах для risk-V уже вшит размер 32)

На риске запускался как в инструкции, пусть тут тоже лежит:

Модули следующие:

Обязательно нужен:
module load gcc-9.5.0

Для risk-V:
module load gcc-riscv64-15.1.0
Для x86:
module load compiler/2023.0.0
module load mkl/2023.0.0 (если REFERENCE_TEST == mkl)


Строка:

srun -N 1 -p k1 ./LU_Benchmark

Замеры на risk-V:

export OMP_NUM_THREADS=8
srun -N 1 -p k1 numactl -C 0-7 ./LU_Benchmark --size 3000 --dac